@model IEnumerable<CIED.Models.TipoApunte>
<main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
    @{
        ViewData["Title"] = "Tipo de Apunte";
    }

    <h2>Tipo de Apunte</h2>

    <p>
        <a asp-action="Create">Agregar</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Descripcion)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Egreso)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Estatus)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descripcion)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Egreso)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Estatus)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.TipoApunteID">Editar</a> |
                        <a asp-action="Details" asp-route-id="@item.TipoApunteID">Detalle</a> |
                        <a asp-action="Delete" asp-route-id="@item.TipoApunteID">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    </main>